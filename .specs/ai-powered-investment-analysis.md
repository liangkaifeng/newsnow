# 智能投资分析系统 - AI Powered Investment Analysis

## 📋 需求概述

将 CapitalFlow 从资讯聚合平台升级为**智能投资决策辅助系统**，提供：
1. 新闻聚合去重与深度分析
2. 全面的市场数据（资金流向、人气指标）
3. AI 驱动的投资机会分析和推荐

## 🎯 核心功能

### 1. 新闻智能聚合（News Aggregation & Deduplication）

#### 问题
- 多个渠道报道相同事件，内容重复
- 缺少深入分析和关联信息
- 无法快速把握事件全貌

#### 解决方案

**1.1 相似新闻检测与聚合**
```typescript
// 新增数据结构
interface NewsCluster {
  id: string
  title: string              // 聚合标题
  mainStory: NewsItem        // 主要报道
  relatedStories: NewsItem[] // 相关报道
  sentiment: 'positive' | 'negative' | 'neutral'
  keywords: string[]         // 关键词
  entities: {                // 相关实体
    stocks: string[]         // 股票代码
    sectors: string[]        // 板块
    companies: string[]      // 公司
  }
  aiSummary?: string         // AI 生成的摘要
  impactScore: number        // 影响力评分 0-100
  createdAt: number
  updatedAt: number
}
```

**1.2 实现技术栈**
- **文本相似度算法**：
  - TF-IDF + 余弦相似度（快速）
  - Sentence Transformers（精准，需服务端）
- **去重策略**：
  - 24小时内相似度 > 85% 的新闻聚合
  - 保留最早/最权威的作为主报道
- **关键词提取**：
  - jieba 中文分词 + TF-IDF
  - 自定义金融词典（股票代码、公司名）

**1.3 UI/UX 设计**
```
📰 某公司发布Q3财报超预期（3条相关报道）
  💰 影响力：⭐⭐⭐⭐⭐ 85/100
  📊 涉及：股票 600519 | 板块 白酒

  📝 AI 摘要：
  该公司Q3营收同比增长23%，净利润增长31%，超市场预期。
  主要受益于高端产品销量提升和成本控制优化。分析师上调
  目标价至XXX元，维持买入评级。

  🔗 相关报道：
  ├─ 财联社：XX公司Q3财报点评（2分钟前）
  ├─ 雪球：深度解读财报亮点（5分钟前）
  └─ 华尔街见闻：机构调高目标价（10分钟前）
```

---

### 2. 市场数据完善（Market Data Enhancement）

#### 2.1 资金流向数据

**数据源选择**：

| 数据类型 | 推荐数据源 | API示例 | 更新频率 |
|---------|-----------|---------|---------|
| 主力资金流向 | 东方财富、同花顺 | `/api/data/capital-flow` | 实时 |
| 北向资金 | Wind、东财 | `/api/data/northbound` | 实时 |
| 龙虎榜 | 东方财富 | `/api/data/dragon-tiger` | 每日 |
| ETF资金流 | Choice、Wind | `/api/data/etf-flow` | 实时 |
| 融资融券 | 交易所官方 | `/api/data/margin` | 每日 |

**数据结构**：
```typescript
interface CapitalFlow {
  stockCode: string
  stockName: string
  mainInflow: number      // 主力净流入（元）
  retailInflow: number    // 散户净流入
  institutionInflow: number // 机构净流入
  foreignInflow: number   // 外资净流入
  changePercent: number   // 涨跌幅
  turnoverRate: number    // 换手率
  timestamp: number
}

interface NorthboundFlow {
  date: string
  netInflow: number       // 净流入（亿元）
  topBuys: Array<{        // 前10大买入
    stockCode: string
    stockName: string
    amount: number
  }>
  topSells: Array<{       // 前10大卖出
    stockCode: string
    stockName: string
    amount: number
  }>
}
```

#### 2.2 人气数据

**数据源**：

| 平台 | 指标 | API | 说明 |
|-----|------|-----|------|
| 雪球 | 关注度、讨论热度 | 爬虫/API | 社区情绪 |
| 东方财富吧 | 帖子数、阅读数 | 爬虫 | 散户热度 |
| 同花顺 | 人气排行 | API | 综合人气 |
| 微博 | 话题热度 | API | 舆情监控 |

```typescript
interface StockPopularity {
  stockCode: string
  stockName: string
  platforms: {
    xueqiu: {
      followers: number      // 关注人数
      posts24h: number       // 24h帖子数
      sentiment: number      // 情绪指数 -1 到 1
    }
    eastmoney: {
      views24h: number       // 24h阅读数
      posts24h: number
    }
    weibo: {
      mentions24h: number    // 提及次数
      hotIndex: number       // 热度指数
    }
  }
  totalScore: number         // 综合人气评分 0-100
  trend: 'rising' | 'falling' | 'stable'
  rankChange: number         // 排名变化
}
```

---

### 3. AI 投资分析（AI Investment Analysis）

#### 3.1 大模型选型

| 模型 | 优势 | 劣势 | 成本 | 推荐度 |
|------|------|------|------|--------|
| **GPT-4** | 分析能力强、上下文长 | 贵 | $$$ | ⭐⭐⭐⭐ |
| **Claude 3.5** | 推理能力优秀 | API限制 | $$ | ⭐⭐⭐⭐⭐ |
| **DeepSeek** | 便宜、中文好 | 能力略弱 | $ | ⭐⭐⭐⭐ |
| **Qwen** | 免费、本地部署 | 需算力 | Free | ⭐⭐⭐ |

**推荐方案**：Claude 3.5 Sonnet（主力）+ DeepSeek（降本）

#### 3.2 AI 分析功能

**3.2.1 新闻摘要与情感分析**
```typescript
interface AISummary {
  summary: string           // 200字摘要
  keyPoints: string[]       // 3-5个要点
  sentiment: {
    score: number           // -1 到 1
    label: 'bearish' | 'neutral' | 'bullish'
    confidence: number      // 置信度 0-1
  }
  relatedStocks: Array<{
    code: string
    impact: 'positive' | 'negative' | 'neutral'
    reason: string
  }>
}

// Prompt 示例
const SUMMARY_PROMPT = `
你是一位资深投资分析师。请分析以下财经新闻：

新闻内容：
{newsContent}

请提供：
1. 核心观点摘要（200字以内）
2. 3-5个关键要点
3. 情感倾向（看多/中性/看空）及理由
4. 受影响的股票/板块及影响方向

以JSON格式输出。
`
```

**3.2.2 板块机会分析**
```typescript
interface SectorAnalysis {
  sectorName: string
  recommendation: 'strong_buy' | 'buy' | 'hold' | 'sell'
  score: number              // 0-100
  reasons: string[]          // 推荐理由
  risks: string[]            // 风险提示
  catalysts: string[]        // 催化剂
  relatedNews: NewsCluster[] // 相关新闻
  capitalFlow: {             // 资金面
    trend: 'inflow' | 'outflow'
    amount: number
  }
  momentum: {                // 动量
    priceChange7d: number
    volumeChange7d: number
  }
}

// Prompt 示例
const SECTOR_ANALYSIS_PROMPT = `
基于以下数据分析【{sectorName}】板块的投资机会：

1. 近期新闻：
{recentNews}

2. 资金流向：
- 7日净流入：{capitalFlow}亿元
- 换手率：{turnoverRate}%

3. 技术面：
- 7日涨跌幅：{priceChange}%
- 成交量变化：{volumeChange}%

请提供：
1. 投资评级（强烈推荐/推荐/观望/规避）
2. 核心逻辑（3-5点）
3. 风险提示
4. 关键催化剂

以JSON格式输出。
`
```

**3.2.3 个股推荐**
```typescript
interface StockRecommendation {
  stockCode: string
  stockName: string
  recommendation: 'strong_buy' | 'buy' | 'hold' | 'sell'
  targetPrice?: number       // 目标价
  currentPrice: number
  upside: number             // 上涨空间 %

  analysis: {
    fundamentals: {          // 基本面
      score: number
      highlights: string[]
    }
    technicals: {            // 技术面
      score: number
      signals: string[]
    }
    sentiment: {             // 市场情绪
      score: number
      trend: string
    }
    catalysts: string[]      // 近期催化剂
  }

  risks: string[]            // 风险因素
  aiInsight: string          // AI 深度洞察
  confidenceLevel: number    // 推荐置信度 0-100
}
```

---

## 🏗️ 技术架构

### 架构图
```
┌─────────────────────────────────────────────────────────┐
│                     Frontend (React)                     │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌─────────┐ │
│  │ 新闻聚合 │  │ 数据看板 │  │ AI分析   │  │ 个股页  │ │
│  └──────────┘  └──────────┘  └──────────┘  └─────────┘ │
└─────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────┐
│                  API Layer (Nitro)                       │
│  ┌────────────┐  ┌────────────┐  ┌──────────────────┐  │
│  │ News API   │  │ Data API   │  │ AI Analysis API  │  │
│  └────────────┘  └────────────┘  └──────────────────┘  │
└─────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────┐
│                   Services Layer                         │
│  ┌──────────────┐ ┌──────────────┐ ┌─────────────────┐ │
│  │ News         │ │ Market Data  │ │ AI Service      │ │
│  │ Aggregator   │ │ Collector    │ │ (Claude/GPT)    │ │
│  └──────────────┘ └──────────────┘ └─────────────────┘ │
└─────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────┐
│                    Data Sources                          │
│  财联社 | 雪球 | 东财 | 同花顺 | Wind | 交易所 | 社交媒体 │
└─────────────────────────────────────────────────────────┘
```

### 数据库设计（SQLite/D1）

```sql
-- 新闻聚合表
CREATE TABLE news_clusters (
  id TEXT PRIMARY KEY,
  title TEXT NOT NULL,
  main_story_id TEXT,
  keywords TEXT,              -- JSON array
  entities TEXT,              -- JSON: stocks, sectors, companies
  sentiment TEXT,
  impact_score INTEGER,
  ai_summary TEXT,
  created_at INTEGER,
  updated_at INTEGER
);

-- 新闻关联表
CREATE TABLE news_cluster_relations (
  cluster_id TEXT,
  news_id TEXT,
  is_main BOOLEAN,
  PRIMARY KEY (cluster_id, news_id)
);

-- 资金流向数据
CREATE TABLE capital_flow (
  stock_code TEXT,
  date TEXT,
  main_inflow REAL,
  retail_inflow REAL,
  institution_inflow REAL,
  foreign_inflow REAL,
  timestamp INTEGER,
  PRIMARY KEY (stock_code, date)
);

-- 人气数据
CREATE TABLE stock_popularity (
  stock_code TEXT,
  date TEXT,
  platform TEXT,              -- xueqiu, eastmoney, etc.
  metrics TEXT,               -- JSON
  total_score INTEGER,
  rank INTEGER,
  PRIMARY KEY (stock_code, date, platform)
);

-- AI 分析缓存
CREATE TABLE ai_analysis_cache (
  key TEXT PRIMARY KEY,
  type TEXT,                  -- summary, sector, stock
  content TEXT,               -- JSON
  expires_at INTEGER,
  created_at INTEGER
);
```

---

## 📅 实施计划

### Phase 1: 新闻智能聚合（2-3周）
- [ ] 实现文本相似度算法
- [ ] 开发新闻聚合引擎
- [ ] 集成 AI 摘要功能（Claude API）
- [ ] UI：聚合新闻卡片展示
- [ ] 测试与优化

### Phase 2: 市场数据集成（2-3周）
- [ ] 接入资金流向数据源（东财API）
- [ ] 接入人气数据（雪球、东财吧爬虫）
- [ ] 北向资金、龙虎榜数据
- [ ] 数据存储与缓存策略
- [ ] UI：数据看板页面

### Phase 3: AI 投资分析（3-4周）
- [ ] 设计 AI 分析 Prompts
- [ ] 实现板块机会分析
- [ ] 实现个股推荐引擎
- [ ] 多维度评分系统
- [ ] UI：AI 分析报告页面
- [ ] 成本优化（缓存策略）

### Phase 4: 优化与扩展（持续）
- [ ] 实时数据推送（WebSocket）
- [ ] 个性化推荐算法
- [ ] 用户自选股跟踪
- [ ] 移动端优化
- [ ] 性能监控与优化

---

## 💰 成本估算

### API 调用成本（月）
- Claude 3.5 API：约 ￥500-1000（预估1万次调用）
- 数据源接口：
  - 东方财富API：免费/￥200
  - 雪球数据爬虫：服务器成本 ￥100
- 服务器/CDN：￥200-500

**总计**：约 ￥1000-2000/月

### 降本策略
1. **智能缓存**：相同新闻24小时内不重复分析
2. **分级调用**：
   - 热门新闻/板块：使用 Claude
   - 一般内容：使用 DeepSeek
3. **批量处理**：合并多个请求降低调用次数
4. **用户付费**：高级功能订阅制（￥29/月）

---

## 🎯 核心竞争力

1. **信息质量**：去重聚合 + AI 深度分析，节省用户时间
2. **数据全面**：资金流向 + 人气数据 + 新闻，多维度决策
3. **智能推荐**：AI 驱动的板块/个股机会分析
4. **实时性**：分钟级数据更新，把握市场脉搏

---

## 🚀 快速启动（MVP）

先实现最小可行产品验证想法：

**Week 1-2: AI 新闻摘要**
- 集成 Claude API
- 为每条重要新闻生成摘要
- 简单的相似新闻检测

**Week 3-4: 基础数据看板**
- 接入东财资金流向API
- 展示主力资金流入TOP10
- 北向资金实时数据

**Week 5-6: 每日AI推荐**
- 每日生成板块机会分析（Top 5）
- 个股推荐列表（Top 10）
- 简单的评分展示

---

## 📊 成功指标

- 新闻去重率：>50%
- AI 摘要准确度：用户满意度 >80%
- 推荐命中率：30天涨幅 >5% 的比例 >60%
- 用户留存：次日留存 >40%，7日留存 >25%

---

## ⚠️ 风险与挑战

1. **数据源稳定性**：爬虫可能被封，需要备用方案
2. **AI 成本**：用户增长后费用激增，需优化策略
3. **合规风险**：投资建议可能涉及金融监管，需免责声明
4. **准确性**：AI 分析不保证盈利，需风险提示
5. **竞争压力**：雪球、东财等巨头已有类似功能

---

## 📝 免责声明模板

```
⚠️ 重要提示

本平台提供的所有信息、数据、分析仅供参考，不构成任何投资建议。
AI 分析基于历史数据和算法模型，不能预测未来市场走势。

投资有风险，入市需谨慎。请独立判断并自行承担投资风险。

本平台不对因使用本服务产生的任何损失承担责任。
```

---

## 🔗 参考资源

- [东方财富API文档](http://datainterface.eastmoney.com/)
- [雪球数据爬虫](https://github.com/search?q=xueqiu+crawler)
- [Claude API](https://docs.anthropic.com/claude/reference/getting-started-with-the-api)
- [金融NLP工具](https://github.com/jindongwang/transferlearning/blob/master/data/dataset.md#finance)

---

**更新日期**: 2025-11-09
**负责人**: liangkaifeng
**优先级**: P0 - 核心功能
**状态**: 📋 规划中
