# Cloudflare Pages 更新部署指南

## 问题

Cloudflare Pages 部署的是旧版本代码，看不到新添加的新闻源。

---

## 🎯 快速解决方案

### 步骤 1: 在 GitHub 创建 main 分支

1. 访问: https://github.com/liangkaifeng/newsnow

2. 点击左上角的**分支下拉菜单**（显示当前分支名）

3. 在输入框输入 `main`

4. 点击 **"Create branch: main from 'claude/setup-forked-project-011CUwSjRkkzC5MMNQCjupAV'"**

5. 设为默认分支:
   - **Settings** → **Branches**
   - **Default branch** 切换到 `main`
   - 点击 **Update** 并确认

### 步骤 2: 配置 Cloudflare Pages

1. 登录 [Cloudflare Dashboard](https://dash.cloudflare.com)

2. **Workers & Pages** → 选择你的项目 `newsnow-ao2`

3. **Settings** → **Builds & deployments**

4. 找到 **Production branch** 设置

5. 确保选择的是 `main` 分支

6. 保存设置

### 步骤 3: 触发重新部署

**方法 A: 自动部署（推荐）**
- Cloudflare Pages 会自动检测 main 分支更新
- 等待 2-3 分钟自动部署

**方法 B: 手动触发**
1. Cloudflare Pages → **Deployments**
2. 点击最新部署右侧的 **⋯** 菜单
3. 选择 **Retry deployment**
4. 等待构建完成

### 步骤 4: 验证部署

1. 等待构建完成（约 2-3 分钟）

2. 访问: https://newsnow-ao2.pages.dev

3. 检查是否出现新分类和新闻源:
   - **科技** 分类下应有 **BestBlogs**
   - 应出现新的 **加密货币** 分类
   - **加密货币** 下应有 5 个源:
     - CoinDesk
     - Cointelegraph
     - Decrypt
     - 金色财经
     - BlockBeats

---

## 🔧 检查部署版本

### 方法 1: 查看分类

访问网站，检查导航栏是否包含 **"加密货币"** 分类。

### 方法 2: 查看新闻源列表

在网站上检查各个分类，确认新源是否显示。

### 方法 3: 查看部署日志

1. Cloudflare Pages → **Deployments**
2. 点击最新的部署
3. 查看 **Build log**
4. 确认构建使用的是正确的分支和提交

---

## 📋 常见问题

### Q1: 部署完成但还是看不到新源？

**A**: 清除浏览器缓存:
- Chrome: `Ctrl + Shift + R` (Windows) 或 `Cmd + Shift + R` (Mac)
- 或者无痕模式访问

### Q2: Cloudflare Pages 显示构建失败？

**A**: 查看构建日志:
1. **Deployments** → 点击失败的部署
2. 查看 **Build log** 找到错误信息
3. 常见错误:
   - 依赖安装失败 → 检查 `package.json`
   - 构建命令错误 → 检查 **Build command**
   - 环境变量缺失 → 添加 `CF_PAGES=1`

### Q3: 构建成功但功能不正常？

**A**: 检查环境变量:
1. **Settings** → **Environment variables**
2. 确保设置了 `CF_PAGES=1`
3. 如果需要登录功能，添加:
   - `G_CLIENT_ID`
   - `G_CLIENT_SECRET`
   - `JWT_SECRET`

### Q4: 如何查看当前部署的代码版本？

**A**:
1. **Deployments** → 最新部署
2. 查看 **Source** 部分
3. 显示部署的分支和 commit hash

---

## 🚀 部署配置检查清单

- [ ] GitHub 上已创建 main 分支
- [ ] main 分支包含最新代码（所有新源）
- [ ] Cloudflare Pages 配置:
  - [ ] Production branch: `main`
  - [ ] Build command: `npm run build`
  - [ ] Build output: `dist/output/public`
  - [ ] Environment variables: `CF_PAGES=1`
- [ ] 触发了重新部署
- [ ] 部署成功（绿色对勾）
- [ ] 访问网站验证新源显示

---

## 📞 需要帮助？

如果按照以上步骤操作后仍然有问题：

1. 提供部署日志截图
2. 提供当前分支信息
3. 提供构建错误信息（如果有）

我会帮你进一步排查！

---

## 🔗 相关文档

- [Cloudflare Pages 文档](https://developers.cloudflare.com/pages/)
- [部署指南](./DEPLOYMENT.zh-CN.md)
- [项目设置](./SETUP.zh-CN.md)
